
# Trabajo Práctico Nº 5 — SVM

**Curso:** Taller de Procesamiento de Señales — 1er Cuatrimestre 2026  
**Restricción de imports:** `numpy`, `matplotlib.pyplot` y `qpsolvers.solve_qp` únicamente. Todo lo demás debe ser implementación propia.

---

## Summary

Trabajo práctico sobre Support Vector Machines (SVM). Se implementa la clase `SVM` resolviendo el problema dual de optimización cuadrática mediante `solve_qp`, con kernels lineal, polinómico y RBF. Se aplica a un problema de control de calidad de circuitos integrados.

---

## Contexto del Problema

El gerente de producción de una fábrica de circuitos integrados desea predecir si un determinado integrado pasará el control de calidad. El archivo `ControlDeCalidad.txt` posee datos de la evaluación de dos pruebas diagnóstico de diferentes integrados, y una tercera columna que indica si pasaron el mencionado control (1 significa pasar la inspección).

---

## Requerimientos

### REQ-001 — Formulación del Problema Dual

Explicar cómo deben ser confeccionadas las matrices para implementar, utilizando `solve_qp`, el problema dual de SVM.

### REQ-002 — Implementación de la Clase SVM

Implementar una clase `SVM` que resuelva el problema dual. El código debe estar estructurado de la siguiente manera:

```python
class SVM:
    # Inicializar atributos y declarar hiperparámetros.
    def __init__(self, kernel, ...):

    # Etapa de entrenamiento.
    def fit(self, X, y):

    # Etapa de testeo hard.
    def predict(self, X):

    # Etapa de testeo soft.
    def predict_soft(self, X):
```

donde `kernel(x1, x2, γ)` es la función que define el kernel (γ es un hiperparámetro). El método `predict_soft` hace referencia a la función $z(x)$ que es utilizada para clasificar como $z(x) \lessgtr 0$.

> **Nota:** Es recomendable que la función kernel pueda funcionar con conjuntos de datos. Por ejemplo, si $x_1 \in \mathbb{R}^{n \times d_x}$ y $x_2 \in \mathbb{R}^{m \times d_x}$, la función debe devolver una matriz $K \in \mathbb{R}^{n \times m}$.

### REQ-003 — Entrenamiento con Kernel Lineal

Entrenar un clasificador SVM para la tarea de control de calidad utilizando kernel lineal. Indicar el accuracy de entrenamiento y el margen unilateral.

### REQ-004 — Visualización de la Frontera de Decisión

Graficar la frontera de decisión aprendida sobre un scatter de los datos utilizando `contour` o `contourf`. Destacar los márgenes y los vectores soporte. ¿Por qué se habla de curvas de nivel?

### REQ-005 — Kernel Polinómico

Repetir los incisos REQ-003 y REQ-004 para un kernel polinómico de la forma:

$$k(x_1, x_2) = (\gamma \cdot x_1^T x_2)^3, \quad \gamma = 0.3$$

### REQ-006 — Kernel RBF

Repetir los incisos REQ-003 y REQ-004 para un kernel RBF de la forma:

$$k(x_1, x_2) = e^{-\gamma \|x_1 - x_2\|^2}, \quad \gamma = 1.0$$

### REQ-007 — Comparación y Conclusiones

Comparar los resultados de los tres kernels y extraer conclusiones.